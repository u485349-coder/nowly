version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: nowly
      POSTGRES_USER: nowly
      POSTGRES_PASSWORD: nowly
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
